## windows
```shell
go run .\cmd\testEchoDevice -server 公网服务器IP:8308 -message "FRP UDP 回显测试"
```

## linux
```shell
go run ./cmd/testEchoDevice -server 公网服务器IP:8308 -message "FRP UDP 回显测试"
```